#version 430

layout(std140) uniform glob {
	mat4 V;
	mat4 iV;
	mat4 P;
	mat4 iP;
	mat4 VP;
	mat4 iVP;
	vec4 cameraPos;
	float time;
	int screen_width;
	int screen_height;
};

layout(local_size_x=32,local_size_y=32,local_size_z=1) in;
layout(rgba8, binding = 0) uniform image2D main_tex;

void main(){
    ivec2 coords = ivec2((gl_WorkGroupID * gl_WorkGroupSize + gl_LocalInvocationID).xy);

	imageStore(main_tex, coords, vec4(0,0,0,0));
}